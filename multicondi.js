// Multi level condition

const price = 8000;

if(price >= 5000){
    // 10% discount
  const discount = price * 10 / 100;
  const payAmount = price - discount;
0
//   o/p declare commands
  console.log(discount);  
  console.log(payAmount);  
}

// Here else is optional . if we don't use else then output will still remain same 

else {console.log(price)

}


const price = 8000;

if(price >= 5000){
    // 10% discount
  const discount = price * 10 / 100;
  const payAmount = price - discount;
0
//   o/p declare commands
  console.log(discount);  
  console.log(payAmount);  
}

// Here else is optional . if we don't use else then output will still remain same 

else {console.log(price)

}